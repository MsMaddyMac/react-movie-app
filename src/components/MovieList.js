import React from 'react'

const MovieList = props => {
  return (
    <>
      {props.movies.map((movie, index) => (

        <div>

        </div>
      ))}
    </>
  )
}

export default MovieList

